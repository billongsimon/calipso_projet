{% extends 'base.html.twig' %}


{% block title %}Hello contact majeur{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <article>
            <p>Numero de la page: {{page.id}}</p>

           <p>Titre de la page:</p>
            {{page.titre}}
            <p>Categorie de la page:</p>
              {{page.categorie.titre}}
              <br>
              <br>
              <br>
             <div class = "contenu">
            <img src="https://via.placeholder.com/300x200" alt="">
             <br>
              <br>
              <br>
            <p>Contenu de la page:</p>
             {{page.contenu}}
            <p>Nom de l'auteur de la page:</p>
             {{page.auteur}}
            <p>Date de creation de la page:</p>
        {{page.createdAt | date('d/m/Y h:i')}}
        <p>Date de modification de la page:</p>
        {{page.jourAt | date('d/m/Y h:i')}}
            </div>       
       </article>   
       </div>
{% endblock %}
