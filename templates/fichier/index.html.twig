{% extends 'base.html.twig' %}

{% block title %}Liste des fichiers{% endblock %}

{% block body %}
    <h1>Fichier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fichier in fichiers %}
            <tr>
                <td>{{ fichier.id }}</td>
                 <td>{{ fichier.titre }}</td>
    <td><a href="{{path('fichier_new', {'id':fichier.id})}}" class="btn btn-info"> Ajouter un fichier </a></td>                                              
    <td><a href="{{path('fichier_delete', {'id':fichier.id})}}" class="btn btn-danger"> Supprimer un fichier </a></td>  
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">aucun resultats trouvés</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fichier_new') }}">Ajout d'un fichier</a>
{% endblock %}


